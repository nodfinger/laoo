# LAOO Meeting

Business feature package and .NET controller module for `LAOO_MEETING`.
Develop and test it through the Root Center host at `8080/5080`; no standalone
Meeting web or API host is supported.

Run the required verification from the repository root:

```powershell
.\tools\scripts\verify-center.ps1 -Module meeting
```
